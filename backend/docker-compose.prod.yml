services:
  api:
    image: ghcr.io/excergic/aware:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8000"
    env_file: /opt/aware/.env
    environment:
      - AWARE_ENVIRONMENT=production
